{use $user, $myTstStdObject}
is_empty:
{is_empty( "" )}

is_array:
{is_array( array(1,2) )}

is_bool:
{is_bool( true )}

is_float:
{is_float( 1.1 )}

is_int:
{is_int( 7 )}

is_bool:
{is_bool( false )}

is_numeric:
{is_numeric( 22 )}

is_object:
{!is_object( 2 )}

is_class:
{is_class( $user, "RaysClass" )}
{is_class( $user, "RaysBeautifulClass" )}

is_instance:
{is_instance( $user, "RaysClass" )}
{is_instance( $user, "RaysBeautifulClass" )}

is_scalar:
{is_scalar( 2 )}

is_string:
{is_string( "hello" )}

is_set:
{var $qasodifjdaosd}{!is_set( $qasodifjdaosd )}

is_constant:
{is_constant( "SEEK_CUR" )}

get_constant:
{get_constant( "SEEK_CUR") }

get_class:
{get_class( $myTstStdObject )}

cast_string:
{cast_string(5.7)}

cast_int:
{cast_int( 5.7 )}

cast_float:
{cast_float(5.7)}
