{use $counter}
{cache_template}
This template is supposed to run twice. 
It requests a property from the counter.

The first time, it returns 1 and 2. 
When it runs the second time it should return 1 and 2 again because the values come from the cache.

Called: {$counter->called}
Called: {$counter->called}

Hello World
