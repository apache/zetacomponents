Solr Schema
===========

Below you can find the schema that defines the base for this component's Solr
configuration (as schema.xml). Modifications are possible, but the field
types, and the dynamic field definitions should *not* be changed.

Default Solr Schema:

.. include:: schema.xml
   :literal:
